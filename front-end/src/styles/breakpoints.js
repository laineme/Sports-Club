const sizes = {
  xs: 320,
  s: 420,
  m: 768,
  l: 992,
  xl: 1200,
};

const breakpoints = {
  xs: `${sizes.xs}px`,
  s: `${sizes.s}px`,
  m: `${sizes.m}px`,
  l: `${sizes.l}px`,
  xl: `${sizes.xl}px`,
};

export { sizes };
export default breakpoints;
